import { styled, theme } from 'stitches.config';

export const SearchFallback = styled('span', {
  fontSize: theme.fontSizes[12],
  color: theme.colors.textLowContrast,
  textAlign: 'center',
  padding: theme.space.paddingRegular,
});
